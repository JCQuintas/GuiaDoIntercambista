<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="FullscreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="child_padding">
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginEnd">16dp</item>
    </style>

    <style name="icon_university">
        <item name="android:tint">@color/gray</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_weight">0</item>
    </style>

    <style name="university_icon_text" parent="TextAppearance.AppCompat.Small">
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">@color/black_text_medium</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="profile_text_body" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/black_text_medium</item>
        <item name="android:layout_marginStart">5dp</item>
        <item name="android:textAlignment">textStart</item>
        <item name="android:maxLength">40</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:ellipsize">none</item>
        <item name="android:scrollHorizontally">false</item>
        <item name="android:maxLines">2</item>
    </style>
</resources>
